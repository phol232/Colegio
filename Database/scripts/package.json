{
  "name": "colegio-database-scripts",
  "private": true,
  "type": "module",
  "scripts": {
    "apply:oltp": "node apply-sql.mjs --oltp",
    "apply:all": "node apply-sql.mjs --oltp"
  },
  "dependencies": {
    "pg": "^8.13.1"
  }
}
